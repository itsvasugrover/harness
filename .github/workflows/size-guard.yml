name: size-guard
on: { push: {}, pull_request: {} }
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x scripts/size-guard.sh && ./scripts/size-guard.sh
